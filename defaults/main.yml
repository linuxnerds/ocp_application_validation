---
become_override: false
ocp_username: user-redhat.com
silent: false
tmp_dir: /tmp/validation_contexts
tmp_kubeconfig: "{{ tmp_dir }}/.kube/config"


ocp_application_validation_list:
  - cluster_name: prod
    host:
    username:
    password:
    types:
      - kind: Route
        name: 
        namespace: 


  - cluster_name: dev
    host:
    username:
    password:
    types:
      - name: route